## SECTION=netsurf_libraries

config LIBCSS
	tristate
	prompt "libcss"
	select LIBPARSERUTILS
	select LIBWAPCAPLET
	help
	  LibCSS is a CSS (Cascading Style Sheet) parser and selection engine, written in C
